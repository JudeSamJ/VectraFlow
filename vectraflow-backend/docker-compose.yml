<<<<<<< HEAD
version: '3.8'

# =============================================================
# AWS CLOUD DEPLOYMENT
#
# All infrastructure is external:
#   PostgreSQL  → AWS RDS
#   Redis       → AWS ElastiCache
#   Storage     → AWS S3
#   Vector DB   → Zilliz Cloud (managed Milvus)
#
# This file runs only the application containers.
# Set all AWS credentials in .env before running.
# =============================================================

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "2"]
    restart: unless-stopped

  celery_worker:
    build: .
    env_file:
      - .env
    command: ["celery", "-A", "app.celery_app.celery_app", "worker", "--loglevel=info", "--concurrency=4"]
    restart: unless-stopped

  celery_beat:
    build: .
    env_file:
      - .env
    command: ["celery", "-A", "app.celery_app.celery_app", "beat", "--loglevel=info"]
    restart: unless-stopped
=======
services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./app:/workspace/app
      - ./uploads:/workspace/uploads
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/synapse
      - REDIS_URL=redis://redis:6379/0
      - SERVICE_TYPE=api
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - synapse_net

  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - ./app:/workspace/app
      - ./uploads:/workspace/uploads
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/synapse
      - REDIS_URL=redis://redis:6379/0
      - SERVICE_TYPE=worker
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - synapse_net

  postgres:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: synapse
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - synapse_net

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - synapse_net

networks:
  synapse_net:

volumes:
  postgres_data:
  redis_data:
>>>>>>> 36515d09bd756a4bdcea6bdae0916842b2e73b8f
